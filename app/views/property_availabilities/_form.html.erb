<br>
<%= form_tag(property_property_availabilities_path(@property), :html => {:class => 'form-horizontal'}) do  %>

  <div class="form-group">
    <%= label_tag(:first_available_date) %>

  <%= date_field_tag :first_available_date%>
  </div>

  <div class="form-group">
    <%= label_tag(:last_available_date) %>
    <%= date_field_tag :last_available_date %>
  </div>
  <br>
  <%= submit_tag "Complete", class: 'btn btn-custom' %>
  <% end %>
